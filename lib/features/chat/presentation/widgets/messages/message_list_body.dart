import 'package:flutter/material.dart';

class MessageListBody extends StatelessWidget {
  const MessageListBody({required this.child, super.key});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
